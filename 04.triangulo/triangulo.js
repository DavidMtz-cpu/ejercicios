"use strict";
(function () {
    function calcular(base, altura) {
        var resustado;
        resustado = base * altura / 2;
        return resustado;
    }
    console.log(calcular(12, 7));
})();
